What happens when you initialize a repository? Why do you need to do it?
 You are creating a new area to store files so they can have commits, you need a working directory to be able to do that. 


You are creating a git repository that is empty. A New directory that is empty with the files you want the repository to have. 

How is the staging area different from the working directory and the repository?
What value do you think it offers?


 The staging area is different because it work as a part of git and commit are added to the file in the staging area. 


How can you use the staging area to make sure you have one commit per logical
change?

You can use git add to add a commit to the file you want to make changes and you can use git diff to compare files. 


What are some situations when branches would be helpful in keeping your history
organized? How would branches help?

Branches are helpful so changes do not have to be made to the master branch, so you can test how your idea work. It keeps ideas orangized 

How do the diagrams help you visualize the branch structure?

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?

What are the pros and cons of Git's automatic merging vs. always doing merges
manually?
